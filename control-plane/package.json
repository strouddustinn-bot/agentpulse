{
  "name": "@agentpulse/control-plane",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run test/billing-adversarial.test.ts && vitest run test/billing-adversarial-claims.test.ts && vitest run test/billing-lifecycle.test.ts && vitest run test/billing-lifecycle-concurrency.test.ts && vitest run test/staging-lifecycle-script.test.ts && vitest run test/staging-lifecycle-callback.test.ts && vitest run test/control-plane.test.ts && vitest run test/migrations.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json",
    "types": "wrangler types",
    "types:check": "node -e \"process.exit(process.versions.node.split('.')[0] >= 22 ? 0 : 1)\" && wrangler types --check",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.18.7",
    "@types/node": "^26.1.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10",
    "wrangler": "4.113.0"
  },
  "overrides": {
    "postcss": "8.5.23",
    "sharp": "0.35.3",
    "undici": "7.29.0",
    "nanoid": "3.3.18"
  }
}
